import { eventType } from './eventType';
import { projectType } from './projectType';
import shopItem from './shopItem';
import { loadingImageType } from './loadingImageType';

export const schema = {
  types: [eventType, projectType, shopItem, loadingImageType],
};
